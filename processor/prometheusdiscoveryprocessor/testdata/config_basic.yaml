receivers:
    nop:

processors:
    prometheus_discovery:
        
exporters:
    nop:

service:
    pipelines:
        metrics:
            receivers: [nop]
            processors: [prometheusdiscovery]
            exporters: [nop]
